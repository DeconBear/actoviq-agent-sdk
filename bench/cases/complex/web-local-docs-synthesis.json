{
  "id": "complex.web.local-docs-synthesis",
  "title": "Migrate code using local API docs",
  "category": "web",
  "runtimeTarget": "parity",
  "instruction": "The local docs under docs/ describe old and current API shapes. Update the client implementation to use the current API version and make npm test pass. Do not delete the docs.",
  "fixture": "bench/fixtures/web/local-docs-synthesis",
  "tags": ["complex", "web", "local-docs", "research"],
  "trials": 1,
  "budget": {
    "maxSeconds": 180,
    "maxToolCalls": 55,
    "maxTokens": 90000
  },
  "goldCommand": "node -e \"const fs=require('fs'); fs.writeFileSync('src/sessionClient.js', 'export function buildSessionPayload(config) {\\n  return {\\n    auth: { token: config.token },\\n    retry: { maxAttempts: config.retryCount ?? 3 },\\n    telemetry: false,\\n  };\\n}\\n');\"",
  "graders": [
    {
      "type": "command",
      "command": "npm test",
      "timeoutMs": 120000
    },
    {
      "type": "file_contains",
      "path": "src/sessionClient.js",
      "text": "maxAttempts"
    }
  ],
  "notes": "WebArena/GAIA-style deterministic local docs case; uses file-backed docs instead of live web."
}
