# Session API v2 迁移说明

v2 请求字段：

- `userId`
- `maxAttempts`
- `retryDelayMs`

v2 服务端入口是 `/v2/session/start`。响应里 session id 优先从 header `x-session-id` 读取。旧字段 `retryCount`、`retryDelay`、`/v1/session` 不应继续出现在实现里。
