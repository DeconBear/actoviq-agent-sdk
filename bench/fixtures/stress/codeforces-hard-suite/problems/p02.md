# P02 - Repair Windows

Inspired by a recent roadworks scheduling problem.

You are given weighted half-open intervals `[l, r)`. Choose a subset of non-overlapping intervals with maximum total weight. Intervals that touch are compatible.

Input:

- `n`
- `n` lines with `l r w`

Output the maximum weight.
