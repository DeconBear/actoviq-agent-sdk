# P05 - Sequence Disorder

Inspired by a recent sequence-counting hard version.

Given an array, count inversions modulo `998244353`.

Input:

- `n`
- `n` integers

Output the inversion count modulo `998244353`.
