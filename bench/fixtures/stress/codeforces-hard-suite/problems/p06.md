# P06 - Cycle Check

Inspired by a recent graph-cycle problem.

An undirected graph is stable if every connected component contains exactly one simple cycle. Determine whether the graph is stable.

Input:

- `n m`
- `m` edges

Output `YES` or `NO`.
