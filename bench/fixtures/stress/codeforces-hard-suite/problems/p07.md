# P07 - Stop Pattern on Tree

Inspired by a recent string/tree problem.

Each tree vertex has a lowercase label. Count vertices whose root-to-vertex label string ends with the target pattern.

Input:

- `n target`
- parents of vertices `2..n`
- labels of vertices `1..n`

Output the count.
