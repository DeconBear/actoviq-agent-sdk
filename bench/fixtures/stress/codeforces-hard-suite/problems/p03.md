# P03 - Quadratic Landing

Inspired by a recent quadratic-jump construction problem.

For each value `d`, find a non-negative integer `x` such that:

```text
x * (x + 1) = d
```

Print `x`, or `-1` if no such integer exists.

Input:

- `q`
- `q` lines with `d`

Output one answer per query.
