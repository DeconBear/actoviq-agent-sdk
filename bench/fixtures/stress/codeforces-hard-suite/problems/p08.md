# P08 - Distinct String Cutting

Inspired by a recent string-cutting problem.

Given a string `s` and an integer `k`, choose at least `k` non-overlapping substrings of equal length. Every chosen substring must contain pairwise distinct characters. Maximize that common length.

Input:

- `s k`

Output the maximum length.
